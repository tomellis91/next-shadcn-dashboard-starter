import { RecentTransactionsSkeleton } from '@/features/overview/components/recent-transactions-skeleton';
import React from 'react';

export default function Loading() {
  return <RecentTransactionsSkeleton />;
}
